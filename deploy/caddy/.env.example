# Public host served by Caddy.
DOMAIN=example.com

# Private upstreams reachable from the Caddy runtime network.
FRONTEND_UPSTREAM=frontend:4173
BACKEND_UPSTREAM=backend:8000

# Backend CORS allowlist. Keep this in backend env as well.
CORS_ALLOW_ORIGINS=https://example.com
